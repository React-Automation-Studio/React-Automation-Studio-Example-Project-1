docker run --detach \
    --restart=always \
    --volume=mdsplus-data:/data \
    --net=host \
    --name=mdsplus-app \
    mdsplus-novnc:latest && \
docker run --rm -it mdsplus-caddy caddy hash-password -plaintext "Ignition!"

hash=JDJhJDEwJGgxNXg5bVB3dTZqcVJYWjJnNlFJVnV4UDU0amppMlpDUVppaGdZWFlCVWNZUktFOVdvcFN5 && \
docker run --detach \
    --restart=always \
    --name=mdsplus-web \
    --env=APP_USERNAME="operator" \
    --env=APP_PASSWORD_HASH=${hash} \
    --net=host \
    mdsplus-caddy


# docker run --rm -it mdsplus-caddy caddy hash-password -plaintext "Ignition!"